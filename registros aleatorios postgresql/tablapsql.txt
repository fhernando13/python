pip install psycopg2


create table abril_mayo(
    numFactura serial primary key,
    numCliente integer, 
    fecha Date, 
    hora time, 
    noVendedor integer, 
    monto integer, 
    iva numeric(10, 2), 
    total numeric(10, 2), 
    tipoPago varchar(100)
);
